MONDAY_API_TOKEN=
DEALS_BOARD_ID=
WORK_ORDERS_BOARD_ID=
GEMINI_API_KEY=

# Optional. Required in production (Vercel) so the board-data cache and
# session store survive between serverless invocations — see DECISION_LOG.md.
# Left unset, both fall back to in-process memory, which is fine for local
# `uvicorn` dev but loses everything on a serverless cold start.
KV_REST_API_URL=
KV_REST_API_TOKEN=
