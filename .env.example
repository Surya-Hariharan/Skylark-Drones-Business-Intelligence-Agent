MONDAY_API_TOKEN=
GEMINI_API_KEY=

# Comma-separated Monday.com board IDs the agent should connect to. Any
# number of boards is supported — each board's kind (deals / work orders) is
# inferred from its own discovered schema, not from which slot it sits in.
# Board IDs are in the board's URL, or Board menu -> More actions -> Developers.
MONDAY_BOARD_IDS=
